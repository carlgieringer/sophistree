// This file sets up the necessary configuration for the bottom sheet to work properly

// Import reanimated first to ensure it's properly initialized
import "react-native-reanimated";

// Initialize gesture handler
import { GestureHandlerRootView } from "react-native-gesture-handler";

// Export components needed by AppBottomSheet
export { GestureHandlerRootView };
